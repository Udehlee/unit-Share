 function User({ id, fullname, email, password, meterNumber, unitBalance }) {
  return {
    id,
    fullname,
    email,
    password,
    meterNumber,
    unitBalance
  };
}


export default { User }